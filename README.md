Use the IGMPLOT, mol-igm.dat file will be generated
Then use the mol-igm.py to run the color-coded plot using matplotlib and numpy
Make sure you have installed this code in the Windows PowerShell
If multiple files are present in the ".dat" format, use the merge.py script 
py .\script.py
